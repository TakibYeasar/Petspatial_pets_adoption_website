# urls.py
from django.urls import path
from .views import *

urlpatterns = [
    # path("pets/", PetListCreateView.as_view(), name="pet-list-create"),
    # path("pets/<int:id>/", PetDetailView.as_view(), name="pet-detail"),
]
